import Login from "../_components/authentication/Login";

export const metadata = {
  title: "Login page",
};

function page() {
  return (
    <div>
      <Login />
    </div>
  );
}

export default page;
